# server-health-monitor
A Python-based server health monitoring and reporting system that collects local and remote server metrics via SSH, generates professional PDF reports, provides a web dashboard, and automates report delivery by email.
